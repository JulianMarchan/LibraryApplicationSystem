@using LibraryApplicationSystem.Web.Models.BookCategories
@using LibraryApplicationSystem.Web.Controllers
@model BookCategoryViewModel


@section scripts{
        <environment names="Development">
            
        <script src="~/view-resources/Views/BookCategories/Index.js" asp-append-version="true"></script>

        </environment>
}
<section class="content-header">
    <div class="container-fluid">
        <div class="row">
            <div class="col-sm-6">
                <h1>@("BookCategory")</h1>
            </div>
            <div class="col-sm-6">
           
            </div>
        </div>
    </div>
</section>
<section class="content">
    <div class="container-fluid">
        <div class="row">
            <div class="col-12">

                <div class="card">
                   
                    <div class="card-header">
                        <a class="btn bg-blue float-sm-right" href="/BookCategories/CreateOrEditBookCategory">
                            <i class="fa fa-plus-square"></i>
                            @("Create BookCategory")
                        </a>
                        <div class="col-sm-6">
                        </div>
                        <div class="row">
                            <div class="col-md-6">
                            </div>
                            <div class="col-md-6">
                            </div>
                        </div>
                    </div>
                    <div class="card-body">
                        <div class="table-responsive">
                            <table id="UsersTable" class="table table-striped table-bordered">
                                <thead>
                                    <tr>
                                        <th>ID</th>
                                        <th>Name</th>
                                        <th>Department</th>

                                        <th style="width: 150px">@L("Actions")</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (var bookCategory in Model.BookCategories)
                                    {
                                        <tr>
                                            <td>@bookCategory.Id</td>
                                            <td>@bookCategory.Name</td>
                                            <td>@bookCategory.Department?.Name</td>

                                            <td>
                                                <button type="button" class="btn btn-sm bg-secondary edit-dept" data-dept-id="@bookCategory.Id">
                                                    <i class="fas fa-pencil-alt"></i> Edit
                                                </button>
                                                <button type="button" class="btn btn-sm bg-danger delete-dept" data-dept-id="@bookCategory.Id" data-user-name="@bookCategory.Name">
                                                    <i class="fas fa-trash"></i> Delete
                                                </button>
                                            </td>
                                        </tr>
                                    }
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
    </section>

                                     